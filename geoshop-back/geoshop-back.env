# Do not set this to True if your server is exposed to internet!
DEBUG=True

POSTGRES_USER="geoshop"
POSTGRES_PASSWORD="geoshop"
POSTGRES_DB="geoshop"

PGDATABASE=geoshop
PGUSER=geoshop
PGHOST=geoshop-back-db
PGPORT=5432
PGPASSWORD=geoshop
PGSCHEMA=geoshop

# The folder where the geodata will be uploaded
MEDIA_ROOT=./files
# A link to public images used in product and metadata
MEDIA_URL=https://example.com/geoshop_media/

ALLOWED_HOST=localhost,geosho-front,geoshop-back,selenium,localhost:8000
# Base URL of the api:
# 1. forces the application to prepend a script name to all URLs generated by the reverse function and the {% url %} template tag
FORCE_SCRIPT_NAME=
# 2. add a prefix to all URL paths in the API
ROOTURL=

DEFAULT_FROM_EMAIL=from
EMAIL_HOST=mail
EMAIL_PORT=587
EMAIL_HOST_USER=user
EMAIL_HOST_PASSWORD=pass
EMAIL_USE_TLS=True
EMAIL_BACKEND="django.core.mail.backends.smtp.EmailBackend"

ADMIN_USERNAME=admin
ADMIN_EMAIL_LIST=admin@example.com

# This is used for CORS and to generate links in email templates
FRONT_URL=localhost:8081
FRONT_HREF=/
FRONT_PROTOCOL=http
CSRF_COOKIE_DOMAIN=localhost
LOGGING_LEVEL=INFO

# Generate this key with Django tools
SECRET_KEY=GenerateOneSecretKey
ADMIN_PASSWORD=Test1234
EXTRACT_USER_PASSWORD=Test1234

AUTO_LEGEND_URL=http://example.com/mapserv?FORMAT=image%2Fpng&TRANSPARENT=true&SERVICE=WMS&VERSION=1.1.1&REQUEST=GetLegendGraphic&LAYER=
# If you plan to manage secret metadata, provide your intranet legend service
INTRA_LEGEND_URL=https://example.com/mapserv_proxy?ogcserver=source+for+image%2Fpng&FORMAT=image%2Fpng&TRANSPARENT=true&SERVICE=WMS&VERSION=1.1.1&REQUEST=GetLegendGraphic&LAYER=

# Only for testing purpose
EMAIL_TEST_TO=test@example.com
DEFAULT_LANGUAGE="de"

# Geometric settings
DEFAULT_SRID=2056

LOCAL_AUTH_ENABLED=True

# OIDC parameters
OIDC_ENABLED=False
OIDC_OP_BASE_URL="https://geoshop-demo-syazg0.zitadel.cloud"
ZITADEL_PROJECT="282430840791683081"
OIDC_RP_CLIENT_ID="285357247964451269"
OIDC_PRIVATE_KEYFILE="/home/arusakov/devel/c2c/extract/geoshop/geoshop-back/keyfile.json"
OIDC_REDIRECT_BASE_URL="http://localhost:8000/"

MAX_ORDER_AREA=0
